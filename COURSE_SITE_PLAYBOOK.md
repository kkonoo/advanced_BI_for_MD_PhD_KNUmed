# 다른 수업 저장소에 재사용하는 사이트 작업 지침

이 문서는 **현재 저장소에 수업을 더 넣는 방법이 아니라**, 별도의 수업 저장소를 새로 열어 동일한 EN/KO·라이트/다크 UI를 적용할 때 새 Codex 채팅에 전달하는 지침입니다.

## 새 채팅용 복사·붙여넣기 프롬프트

아래에서 대괄호 부분만 바꿔 사용합니다.

```text
[TARGET_REPOSITORY_PATH]에 있는 별도의 수업 사이트를 작업해줘.
먼저 저장소의 AGENTS.md, README, _quarto.yml과 기존 qmd/CSS/JS 구조를 확인하고,
기존 수업 내용·URL·프레임워크·사용자 변경을 보존해. Quarto 사이트라면 Quarto를
그대로 사용하고, 다른 프레임워크라면 갈아엎지 말고 같은 기능만 그 구조에 맞게 구현해.

수업 정보
- 영문 수업명: [COURSE_NAME_EN]
- 한국어 수업명: [COURSE_NAME_KO]
- 원본 언어: [EN 또는 KO]
- 문서/주차 범위: [예: Home, W1–W12]
- 이번 번역 범위: [예: Home과 W1–W2]
- 로고·그림 등 자료 위치: [PATH 또는 없음]

적용할 기능과 디자인
1. 각 페이지에 EN/KO 전환을 제공해 같은 주차의 대응 페이지로 이동하게 해.
2. 언어 버튼은 상단 오른쪽의 짙은 남청색(#1d384a) 둥근 캡슐에 EN, KO 순서로
   배치해. 현재 언어는 청록색(#78d0cf) 원형 배경과 흰 글자로 표시하고, 비활성
   언어는 밝은 회청색 글자로 보여줘. 캡슐은 사이트가 라이트 모드여도 흰색으로
   바꾸지 말고 짙은 남청색 표면을 유지해.
3. 언어 캡슐 오른쪽에 같은 짙은 남청색(#1d384a)의 46px 원형 테마 버튼을 분리해
   배치해. 라이트 모드에서는 달(🌙), 다크 모드에서는 해(☀️)를 중앙에 크게 보여주고,
   운영체제 설정과 사용자의 저장된 선택을 존중해.
4. Quarto라면 light: cosmo, dark: darkly와 respect-user-color-scheme: true를 사용하고,
   Quarto의 기본 테마 전환 동작은 유지한 채 CSS로 버튼 모양만 바꿔.
5. 한국어와 영문 사이드바·목차·이전/다음 링크가 각각 올바른 언어와 같은 주차를 가리키게 해.
6. 두 컨트롤 사이는 약 12px 띄워 서로 다른 조작 요소임을 분명히 해. 모바일에서는
   캡슐 40px, 내부 선택 영역 32px, 테마 버튼 40px 정도로 줄이고 메뉴와 겹치지 않게
   해. 키보드 포커스, aria-label, 명암 대비도 유지해.
7. 기존 본문 스타일과 AI 채팅 위젯이 있다면 두 테마에서 모두 읽기 좋게 조정해.

다국어 문서 원칙
- 번역 본문은 브라우저 문자열 치환으로 만들지 말고 언어별 qmd/원본 문서로 관리해.
- Quarto라면 기본적으로 영문 페이지와 동일한 상대 경로를 ko/ 아래에 둬.
  예: chapters/w2.qmd ↔ ko/chapters/w2.qmd.
- 한국어 qmd를 전부 한꺼번에 만들 필요는 없어. 이번 번역 범위만 완성하고,
  아직 번역하지 않은 페이지는 완료본처럼 표시하지 말아줘.
- 번역 시 제목 계층, 명시적 앵커, 코드, 수식, 표, 그림, 링크, 인용, callout 순서를 보존해.
- 전문용어 명칭은 표준 영어 표기를 유지하고 설명 문장은 자연스러운 한국어로 작성해.
  특히 `핵심 용어` 표의 왼쪽 `용어` 열은 영어만, 오른쪽 정의 열은 한국어로 통일하고,
  코드·함수·패키지명은 번역하지 마.
- 그림과 공용 자산은 양 언어에서 공유하고 ko/에 복제하지 마. 상대 경로가 렌더 후에도
  실제 파일을 가리키는지 확인해.

추천 작업 순서
1. 현재 구조와 변경 상태를 읽고 기존 URL·내비게이션·렌더 대상을 파악한다.
2. 라이트/다크 테마와 공용 CSS 변수를 먼저 적용한다.
3. EN/KO 페이지 매핑과 언어별 메타데이터·사이드바를 만든다.
4. Home과 첫 강의 한 개를 시범 번역해 UI와 경로를 검증한다.
5. 문제가 없으면 내가 지정한 나머지 범위만 순서대로 번역한다.
6. 전체 사이트를 렌더하고 양 언어 대응 링크, 이미지, 앵커, 이전/다음 링크,
   데스크톱·모바일 UI를 검증한다.

완료 조건
- 등록된 모든 페이지가 오류 없이 렌더된다.
- EN/KO 버튼이 항상 같은 문서·같은 주차의 대응 언어로 이동한다.
- 한국어 페이지의 HTML lang, 목차 제목, 사이드바, 내비게이션이 한국어로 표시된다.
- 라이트/다크 모드에서 본문, 코드, 표, callout, 위젯의 대비가 적절하다.
- 깨진 내부 링크, 앵커, 이미지가 없다.
- 번역 완료 범위와 아직 번역하지 않은 범위를 최종 보고에 구분한다.

먼저 필요한 파일을 직접 확인하고 합리적인 기본값으로 진행해. 경로 또는 번역 원본처럼
결과를 크게 바꾸는 정보가 실제로 없을 때만 질문해. 커밋·푸시·배포는 내가 별도로
요청할 때만 수행해.
```

## 핵심 판단 기준

- 이 프롬프트는 **수업마다 별도 저장소**를 전제로 합니다. 대상 저장소의 절대 경로를 첫 줄에 넣습니다.
- 각 번역 페이지에는 별도 qmd가 있는 편이 가장 안정적입니다. 다만 모든 주차를 처음부터 복제할 필요는 없으며, 요청한 범위부터 순차적으로 만듭니다.
- CSS, JavaScript, 그림, 코드 예제는 공유하므로 실제로 중복 관리하는 것은 번역이 필요한 본문 qmd뿐입니다.
- 기존 저장소의 구조가 `ko/` 미러 방식과 크게 다르면 URL을 강제로 바꾸지 말고, 대응 페이지 표 또는 해당 프레임워크의 i18n 기능으로 같은 결과를 냅니다.

## UI 수치 참고

- EN/KO 바깥 캡슐: 높이 `46px`, 패딩 `3px`, `border-radius: 999px`
- 각 언어 선택 영역: 최소 너비 `38px`, 높이 `38px`
- 언어 순서: 왼쪽 `EN`, 오른쪽 `KO`
- 캡슐·테마 버튼 표면: `#1d384a` (라이트/다크 모드 공통)
- 활성 언어: 배경 `#78d0cf`, 글자 `#fff`
- 비활성 언어: 밝은 회청색 글자, 별도 배경 없음
- 테마 버튼: `46 × 46px`, 완전한 원형
- 테마 아이콘: 중앙 정렬한 달/해 emoji, 약 `23px`
- 언어 캡슐과 테마 버튼 사이: 약 `12px`
- 두 컨트롤의 테두리: `rgba(120, 208, 207, 0.12)` 또는 시각적으로 동등한 매우 옅은 테두리
- 모바일: 바깥 캡슐·테마 버튼 `40px`, 내부 언어 영역 `32px`

핵심 인상은 **짙은 남청색 캡슐 안의 청록색 활성 원 + 같은 색의 독립된 원형 테마
버튼**입니다. 흰색 또는 반투명 유리 재질의 컨트롤로 바꾸지 않습니다. 이 수치는 참고
이미지의 인상을 재현하기 위한 기준이며, 대상 사이트의 기존 내비게이션 높이에 맞춰
소폭 조정할 수 있습니다.
