{
  "hash": "5458e73e1ee051d69cda88318975f887",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Multi-omics Integration & GWAS Gene Prioritization\"\nsubtitle: \"Advanced Bioinformatics for Clinician-Scientists | Week 11\"\nauthor: \"Your Name\"\ndate: today\nformat:\n  revealjs:\n    theme: simple\n    slide-number: true\n    code-copy: true\n    highlight-style: github\n    transition: fade\n    background-transition: fade\n    footer: \"Advanced Bioinformatics | W11 — Multi-omics Integration\"\n    css: custom.css\nexecute:\n  echo: true\n  eval: false\n---\n\n# The Full Picture {background-color=\"#1D9E75\"}\n\n---\n\n## What We've Built So Far\n\n```\nW2–W3   GWAS / PRS       → genetic variants associated with disease\nW4–W5   Epigenomics      → regulatory landscape (methylation, chromatin)\nW6–W7   Metagenomics     → microbial community structure & function\nW8–W9   scRNA-seq        → cell-type-specific expression & dynamics\nW10     Spatial          → tissue-level context\n```\n\n::: {.fragment}\nEach layer tells a **partial story**.\n\n> **Multi-omics integration** combines these layers to find  \n> convergent, cross-validated biological signals.\n:::\n\n---\n\n## Why Integration?\n\n:::: {.columns}\n::: {.column width=\"50%\"}\n**Single-omics**\n\n- High-dimensional\n- Noisy\n- Incomplete picture\n- Many false positives\n:::\n::: {.column width=\"50%\"}\n**Multi-omics**\n\n- Cross-layer validation\n- More robust signals\n- Mechanistic links\n- Better biomarkers\n:::\n::::\n\n::: {.fragment}\n> A gene that is a GWAS hit **and** differentially methylated **and** overexpressed in a specific cell type  \n> is far more likely to be causally relevant than any one signal alone.\n:::\n\n---\n\n## Integration Strategies\n\n| Strategy | Approach | Best for |\n|---------|---------|---------|\n| **Correlation** | Pairwise association between layers | Exploratory, hypothesis generation |\n| **MOFA2** | Factor analysis across layers | Discovering shared variation |\n| **mixOmics** | Supervised multiblock methods | Prediction, biomarker discovery |\n| **DIABLO** | Supervised multi-omics | Classification with omics data |\n| **SNF** | Similarity network fusion | Patient subtyping |\n| **eQTL / colocalization** | GWAS + expression | Gene prioritization |\n\n---\n\n# MOFA2 — Multi-Omics Factor Analysis {background-color=\"#534AB7\"}\n\n---\n\n## The MOFA2 Idea\n\n```\nOmics layer 1: RNA-seq    (n_samples × n_genes)\nOmics layer 2: Methylation (n_samples × n_CpGs)\nOmics layer 3: Metabolomics (n_samples × n_metabolites)\n         ↓\nMOFA2: find latent factors that explain variance ACROSS layers\n\nFactor 1 → driven by cell cycle genes + methylation changes\nFactor 2 → driven by immune activity in RNA + microbiome\nFactor 3 → driven by metabolic genes + metabolites\n```\n\n::: {.fragment}\nEach factor = a **biological axis of variation** shared across omics layers\n:::\n\n---\n\n## Building a MOFA2 Model\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(MOFA2)\nlibrary(tidyverse)\nlibrary(here)\n\n# Load each omics layer (same samples, different features)\nrna   <- readRDS(here(\"data\", \"processed\", \"rna_matrix.rds\"))     # genes × samples\nmeth  <- readRDS(here(\"data\", \"processed\", \"meth_matrix.rds\"))    # CpGs × samples\nmicro <- readRDS(here(\"data\", \"processed\", \"microbiome_matrix.rds\")) # taxa × samples\n\n# Create MOFA object\nmofa <- create_mofa(list(\n  RNA          = rna,\n  Methylation  = meth,\n  Microbiome   = micro\n))\n\n# Visualize data overview\nplot_data_overview(mofa)\n```\n:::\n\n\n---\n\n## Set Options & Train\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Model options\nmodel_opts <- get_default_model_options(mofa)\nmodel_opts$num_factors <- 15\n\n# Training options\ntrain_opts <- get_default_training_options(mofa)\ntrain_opts$seed           <- 42\ntrain_opts$convergence_mode <- \"slow\"   # more accurate\n\nmofa <- prepare_mofa(mofa,\n                     model_options   = model_opts,\n                     training_options = train_opts)\n\n# Train (takes ~5–10 min)\nmofa <- run_mofa(mofa,\n                 outfile = here(\"results\", \"mofa_model.hdf5\"))\n\n# Load pre-trained model\nmofa <- load_model(here(\"results\", \"mofa_model.hdf5\"))\n```\n:::\n\n\n---\n\n## Inspect Factors\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Variance explained per factor per view\nplot_variance_explained(mofa, max_r2 = 15)\n\n# Factor values per sample (colored by metadata)\nplot_factor(mofa,\n            factor  = 1:5,\n            color_by = \"diagnosis\",\n            shape_by = \"batch\")\n\n# Which features drive each factor?\nplot_top_weights(mofa,\n                 view   = \"RNA\",\n                 factor = 1,\n                 nfeatures = 20)\n```\n:::\n\n\n---\n\n## Associating Factors with Clinical Variables\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Add sample metadata\nsamples_metadata(mofa) <- sample_metadata   # data.frame with sample info\n\n# Correlation of factors with clinical variables\ncorrelate_factors_with_covariates(\n  mofa,\n  covariates  = c(\"age\", \"bmi\", \"disease_duration\",\n                  \"CRP\", \"diagnosis\"),\n  plot        = \"log_pval\"\n)\n\n# Factor 3 correlates with CRP → inflammatory factor?\n# Inspect top RNA features in Factor 3\nget_weights(mofa, view = \"RNA\", factor = 3) |>\n  as_tibble(rownames = \"gene\") |>\n  arrange(desc(abs(value))) |>\n  head(20)\n```\n:::\n\n\n---\n\n# mixOmics / DIABLO — Supervised Integration {background-color=\"#185FA5\"}\n\n---\n\n## DIABLO — Multi-block Discriminant Analysis\n\n```\nGoal: find a combination of features across omics layers\n      that best discriminates between groups (e.g., responder vs. non-responder)\n\nInput:\n  X = list of omics matrices (RNA, methylation, proteomics)\n  Y = outcome variable (factor: responder / non-responder)\n\nOutput:\n  - Selected features per layer\n  - Correlation structure between layers\n  - Classification model\n```\n\n---\n\n## Running DIABLO\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(mixOmics)\n\n# Input data\nX <- list(\n  RNA    = t(rna_filtered),      # samples × features\n  Methyl = t(meth_filtered),\n  Micro  = t(micro_filtered)\n)\nY <- factor(sample_metadata$response)   # \"Responder\" / \"Non-responder\"\n\n# Design matrix — how correlated should layers be?\n# 1 = maximize inter-layer correlation\n# 0 = maximize discrimination only\ndesign <- matrix(0.1,\n                  ncol = length(X),\n                  nrow = length(X),\n                  dimnames = list(names(X), names(X)))\ndiag(design) <- 0\n\n# Initial DIABLO run\ndiablo <- block.splsda(X, Y,\n                        ncomp  = 5,\n                        design = design)\n```\n:::\n\n\n---\n\n## Tuning & Final Model\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Tune number of features to select per layer\nset.seed(42)\nperf_diablo <- perf(diablo,\n                     validation    = \"Mfold\",\n                     folds         = 5,\n                     nrepeat       = 10,\n                     dist          = \"centroids.dist\")\n\n# Optimal number of components\nplot(perf_diablo)\nncomp_opt <- perf_diablo$choice.ncomp$WeightedVote[\"Overall.BER\",\n                                                     \"centroids.dist\"]\n\n# Tune number of features\ntest_keepX <- list(RNA    = c(5, 10, 15, 20),\n                   Methyl = c(5, 10, 15),\n                   Micro  = c(3, 5, 10))\n\ntune_diablo <- tune.block.splsda(X, Y,\n                                  ncomp      = ncomp_opt,\n                                  test.keepX = test_keepX,\n                                  design     = design,\n                                  validation = \"Mfold\",\n                                  folds      = 5,\n                                  nrepeat    = 10)\n\n# Final model\nfinal_diablo <- block.splsda(X, Y,\n                              ncomp    = ncomp_opt,\n                              keepX    = tune_diablo$choice.keepX,\n                              design   = design)\n```\n:::\n\n\n---\n\n## DIABLO Visualizations\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Circos plot — cross-layer correlations of selected features\ncircosPlot(final_diablo,\n           cutoff        = 0.7,\n           line          = TRUE,\n           color.blocks  = c(\"#1D9E75\", \"#534AB7\", \"#D85A30\"),\n           color.cor     = c(\"#D85A30\", \"#1D9E75\"))\n\n# Sample plot\nplotDiablo(final_diablo, ncomp = 1)\n\n# Heatmap of selected features\ncimDiablo(final_diablo,\n          color.blocks = c(\"#1D9E75\", \"#534AB7\", \"#D85A30\"),\n          comp         = 1,\n          margin       = c(8, 20),\n          legend.position = \"topright\")\n```\n:::\n\n\n---\n\n# GWAS Gene Prioritization {background-color=\"#0F6E56\"}\n\n---\n\n## The Problem: GWAS Hit → Causal Gene\n\nA GWAS locus typically contains:\n- 1 lead SNP\n- Dozens of correlated SNPs in LD\n- Multiple nearby genes\n\nWhich gene is the actual effector?\n\n```\nEvidence layers for gene prioritization:\n├── Positional     → nearest gene\n├── eQTL           → gene whose expression is controlled by the SNP\n├── Chromatin      → SNP overlaps enhancer contacting gene X (Hi-C)\n├── Epigenomic     → SNP in cell-type-specific open chromatin\n├── scRNA-seq      → gene expressed in disease-relevant cell type\n└── Multi-omics    → gene appears across multiple evidence layers\n```\n\n---\n\n## MAGMA — Gene-Level GWAS Analysis\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# MAGMA: aggregates SNP p-values to gene-level scores\n# Run from command line (pre-computed)\nsystem(paste(\n  \"magma\",\n  \"--bfile\",   here(\"data\", \"raw\", \"reference_panel\"),\n  \"--gene-annot\", here(\"data\", \"raw\", \"magma.genes.annot\"),\n  \"--pval\",    here(\"data\", \"raw\", \"gwas_sumstats.txt\"),\n                  \"N=150000\",\n  \"--gene-model\", \"snp-wise=mean\",\n  \"--out\",     here(\"results\", \"magma_gene\")\n))\n\n# Load gene-level results\nmagma_genes <- read_tsv(here(\"results\", \"magma_gene.genes.out\"),\n                         col_types = cols()) |>\n  arrange(P)\n\nhead(magma_genes)\n```\n:::\n\n\n---\n\n## H-MAGMA — Cell-Type-Specific Prioritization\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# H-MAGMA: uses Hi-C contacts from specific cell types\n# to map SNPs to genes via chromatin interactions\n\n# Pre-computed H-MAGMA results for different tissues\n# (neurons, cardiomyocytes, immune cells, etc.)\n\nhmagma_neuron <- read_tsv(\n  here(\"data\", \"processed\", \"hmagma_adult_brain.genes.out\")\n) |> arrange(P)\n\nhmagma_immune <- read_tsv(\n  here(\"data\", \"processed\", \"hmagma_immune.genes.out\")\n) |> arrange(P)\n\n# Compare: different genes prioritized in neurons vs. immune cells\n# for same GWAS → cell-type specificity of disease mechanism\ninner_join(\n  hmagma_neuron  |> select(GENE, P_neuron  = P),\n  hmagma_immune  |> select(GENE, P_immune  = P)\n) |>\n  filter(P_neuron < 0.05 | P_immune < 0.05) |>\n  mutate(cell_type_specific = case_when(\n    P_neuron < 0.05 & P_immune > 0.05 ~ \"Neuron-specific\",\n    P_immune < 0.05 & P_neuron > 0.05 ~ \"Immune-specific\",\n    TRUE ~ \"Both\"\n  ))\n```\n:::\n\n\n---\n\n## Convergent Evidence Scoring\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Combine multiple lines of evidence\n# Each layer contributes points toward gene prioritization\n\ngene_scores <- tibble(gene = all_candidate_genes) |>\n\n  # 1. GWAS: MAGMA gene p-value\n  left_join(magma_genes |> select(gene = GENE, magma_p = P)) |>\n  mutate(score_gwas = -log10(magma_p)) |>\n\n  # 2. eQTL colocalization\n  left_join(coloc_results |> select(gene, pp_h4)) |>\n  mutate(score_coloc = ifelse(pp_h4 > 0.8, 3, 0)) |>\n\n  # 3. scRNA-seq — expressed in disease cell type?\n  left_join(scrna_markers |> select(gene, avg_log2FC)) |>\n  mutate(score_scrna = ifelse(avg_log2FC > 0.5, 2, 0)) |>\n\n  # 4. ATAC-seq — SNP in open chromatin?\n  left_join(atac_overlap |> select(gene, in_peak)) |>\n  mutate(score_atac = ifelse(in_peak, 2, 0)) |>\n\n  # Total convergent score\n  mutate(total_score = score_gwas + score_coloc +\n                       score_scrna + score_atac) |>\n  arrange(desc(total_score))\n\nhead(gene_scores, 20)\n```\n:::\n\n\n---\n\n## Visualizing Convergent Evidence\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(ggplot2)\nlibrary(patchwork)\n\n# Lollipop chart — top prioritized genes\ntop_genes <- gene_scores |> slice_max(total_score, n = 20)\n\nggplot(top_genes,\n       aes(x = total_score,\n           y = reorder(gene, total_score))) +\n  geom_segment(aes(xend = 0, yend = gene),\n               color = \"gray70\", linewidth = 0.8) +\n  geom_point(aes(color = score_coloc > 0,\n                 size  = score_gwas),\n             alpha = 0.9) +\n  scale_color_manual(values = c(\"#888780\", \"#1D9E75\"),\n                     labels  = c(\"No colocalization\",\n                                 \"eQTL colocalized\")) +\n  labs(x     = \"Convergent evidence score\",\n       y     = \"Gene\",\n       color = \"eQTL\",\n       size  = \"GWAS score\",\n       title = \"Multi-evidence gene prioritization\") +\n  theme_minimal()\n```\n:::\n\n\n---\n\n# Hands-On {background-color=\"#BA7517\"}\n\n---\n\n## Dataset: TCGA Multi-omics (BRCA)\n\nTCGA Breast Cancer — RNA-seq + DNA methylation + miRNA\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(TCGAbiolinks)\nlibrary(MOFA2)\nlibrary(tidyverse)\nlibrary(here)\n\n# Download TCGA BRCA multi-omics (pre-queried)\n# RNA-seq, methylation (450K), miRNA\nrna_brca   <- readRDS(here(\"data\", \"raw\", \"tcga_brca_rna.rds\"))\nmeth_brca  <- readRDS(here(\"data\", \"raw\", \"tcga_brca_meth.rds\"))\nmirna_brca <- readRDS(here(\"data\", \"raw\", \"tcga_brca_mirna.rds\"))\n\n# Align samples\ncommon_samples <- Reduce(intersect,\n                          list(colnames(rna_brca),\n                               colnames(meth_brca),\n                               colnames(mirna_brca)))\n\ncat(\"Common samples:\", length(common_samples), \"\\n\")\n```\n:::\n\n\n---\n\n## Hands-On: MOFA2\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Subset to highly variable features per layer\nselect_hv <- function(mat, n = 1000) {\n  rv <- rowVars(mat)\n  mat[order(rv, decreasing = TRUE)[1:n], ]\n}\n\nmofa_obj <- create_mofa(list(\n  RNA   = select_hv(rna_brca[, common_samples],  1000),\n  Methyl = select_hv(meth_brca[, common_samples], 500),\n  miRNA = select_hv(mirna_brca[, common_samples], 200)\n))\n\nmodel_opts            <- get_default_model_options(mofa_obj)\nmodel_opts$num_factors <- 10\n\nmofa_obj <- prepare_mofa(mofa_obj, model_options = model_opts)\nmofa_obj <- run_mofa(mofa_obj, use_basilisk = TRUE)\n\nplot_variance_explained(mofa_obj)\n```\n:::\n\n\n---\n\n## Hands-On: Factor–Subtype Association\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# PAM50 subtypes as metadata\nsamples_metadata(mofa_obj) <- tcga_metadata |>\n  filter(sample %in% common_samples) |>\n  column_to_rownames(\"sample\")\n\n# Which factors separate PAM50 subtypes?\nplot_factor(mofa_obj,\n            factor   = 1:5,\n            color_by = \"PAM50\",\n            dodge    = TRUE,\n            legend   = TRUE)\n\n# Top RNA drivers of Factor 1\nplot_top_weights(mofa_obj,\n                 view      = \"RNA\",\n                 factor    = 1,\n                 nfeatures = 15,\n                 scale     = TRUE)\n```\n:::\n\n\n---\n\n# Next Week {background-color=\"#534AB7\"}\n\n---\n\n## W12 Preview — Network Analysis & Drug Repositioning\n\n**What we'll cover:**\n\n- Protein–protein interaction networks\n- Community detection — finding gene modules\n- Drug target identification from networks\n- Drug repositioning using transcriptomic signatures (CMAP/L1000)\n\n**Install before next class:**\n\n\n::: {.cell}\n\n```{.r .cell-code}\ninstall.packages(c(\"igraph\", \"ggraph\", \"tidygraph\"))\nBiocManager::install(c(\"STRINGdb\", \"fgsea\"))\ninstall.packages(\"cmapR\")   # CMAP/L1000 interface\n```\n:::\n\n\n---\n\n## Key Resources\n\n- **MOFA2** — [biofam.github.io/MOFA2](https://biofam.github.io/MOFA2)\n- **mixOmics / DIABLO** — [mixomics.org](http://www.mixomics.org)\n- **TCGA** — [portal.gdc.cancer.gov](https://portal.gdc.cancer.gov)\n- **H-MAGMA** — [github.com/thewonlab/H-MAGMA](https://github.com/thewonlab/H-MAGMA)\n- **Open Targets** — [platform.opentargets.org](https://platform.opentargets.org) — convergent evidence scoring\n\n---\n\n## Questions? {background-color=\"#1D9E75\"}\n\n::: {.center}\nNext week: **Network Analysis & Drug Repositioning**\n\nCourse materials → [your GitHub Pages URL here]\n:::\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {
      "include-after-body": [
        "\n<script>\n  // htmlwidgets need to know to resize themselves when slides are shown/hidden.\n  // Fire the \"slideenter\" event (handled by htmlwidgets.js) when the current\n  // slide changes (different for each slide format).\n  (function () {\n    // dispatch for htmlwidgets\n    function fireSlideEnter() {\n      const event = window.document.createEvent(\"Event\");\n      event.initEvent(\"slideenter\", true, true);\n      window.document.dispatchEvent(event);\n    }\n\n    function fireSlideChanged(previousSlide, currentSlide) {\n      fireSlideEnter();\n\n      // dispatch for shiny\n      if (window.jQuery) {\n        if (previousSlide) {\n          window.jQuery(previousSlide).trigger(\"hidden\");\n        }\n        if (currentSlide) {\n          window.jQuery(currentSlide).trigger(\"shown\");\n        }\n      }\n    }\n\n    // hookup for slidy\n    if (window.w3c_slidy) {\n      window.w3c_slidy.add_observer(function (slide_num) {\n        // slide_num starts at position 1\n        fireSlideChanged(null, w3c_slidy.slides[slide_num - 1]);\n      });\n    }\n\n  })();\n</script>\n\n"
      ]
    },
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}